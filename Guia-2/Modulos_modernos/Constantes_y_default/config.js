export const URL_API = 'https://api.miapp.com';
export const TIMEOUT = 5000;
export const MAX_RETRIES = 3;

const configuracionCompleta = {
    URL_API,
    TIMEOUT,
    MAX_RETRIES
};

export default configuracionCompleta;