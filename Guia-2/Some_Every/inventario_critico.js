// Tienes un array de productos con nombre, stock y crítico (booleano). Verifica: 
// (a) si algún producto crítico tiene stock 0, (b) si todos los críticos tienen stock > 5, 
// (c) si ningún producto tiene stock negativo. Imprime las tres respuestas con un 
// mensaje claro.

